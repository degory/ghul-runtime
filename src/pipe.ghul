namespace Ghul.Pipes is
    use System.InvalidOperationException
    use System.NotImplementedException

    use System.Text.StringBuilder

    use Collections

    // FIXME: should dispose of all enumerators - ideally somehow check if they really need disposing
    // and so only pay cost of try/finally handler if genuinely needed. For now, in practice, none
    // of the enumerators we want to use actually have any non-managed state that needs to be disposed

    // Rewind `source`, leaving it ready for the next consumer. Only a
    // pipe needs this: a pipe is its own cursor, so a partially walked
    // one stays part way through, and a terminal that stops early has
    // left it there. A plain iterable hands a fresh iterator to whoever
    // asks, so there is nothing to restore, and resetting the iterator
    // a terminal happens to hold would ask a .NET enumerator to rewind,
    // which they are permitted - and compiler-generated ones obliged -
    // to reject.
    rewind[T](source: Iterable[T]) is
        if isa Pipe[T](source) then
            cast Pipe[T](source).reset()
        fi
    si

    // Rewind `source` and hand back a cursor over it, replacing `spent`.
    // A pipe source is rewound in place - which recursively rewinds
    // whatever it draws from - and is its own cursor. A plain iterable
    // cannot be rewound, but nobody shares the iterators it hands out
    // either: `spent` is drained to the end first, because a .NET
    // iterator can share state with its source - `File.read_lines`
    // shares its reader - so a partly walked one cannot simply be
    // dropped, and only then is a fresh iterator taken, the one restart
    // every iterable owes. `spent` is never reset: .NET enumerators are
    // permitted to reject that, and compiler-generated ones must.
    rewind[T](source: Iterable[T], spent: Iterator[T]) -> Iterator[T] is
        if isa Pipe[T](source) then
            let source_pipe = cast Pipe[T](source)

            source_pipe.reset()

            return source_pipe
        fi

        while spent.move_next() do
        od

        return source.iterator
    si

    // Lift an `Iterable[T]` (any producer with a `.iterator` —
    // includes any existing `Pipe[T]`, which is short-circuited via
    // an `isa` check) to `Pipe[T]`. For state-stepping producers,
    // use `stream(initial, advance)` (see `stream.ghul`).

    pipe[T](source: Iterable[T]) -> Pipe[T] pure is
        if isa Pipe[T](source) then
            return cast Pipe[T](source)
        else
            return ADAPTOR_PIPE[T](source)
        fi
    si

    // T can't carry a bound inside Pipe[T] itself, so min/max are free
    // functions rather than trait members - min_by/max_by don't have
    // that problem, since the key type is a fresh per-call parameter.
    min[T: Ghul.Comparable[T]](values: Iterable[T]) -> MAYBE[T] pure is
        let it = values.iterator

        if it.move_next() then
            let result mut = it.current

            while it.move_next() do
                if it.current < result then
                    result = it.current
                fi
            od

            return MAYBE(result)
        fi

        return MAYBE[T]()
    si

    max[T: Ghul.Comparable[T]](values: Iterable[T]) -> MAYBE[T] pure is
        let it = values.iterator

        if it.move_next() then
            let result mut = it.current

            while it.move_next() do
                if it.current > result then
                    result = it.current
                fi
            od

            return MAYBE(result)
        fi

        return MAYBE[T]()
    si

    // A pipe is an iterator that is also iterable, so it can be walked
    // by `for` and passed to the combinators in this namespace. The
    // combinators are the free functions above, called with `|>`; a pipe
    // carries no methods of its own beyond the iterator protocol.
    trait Pipe[T]: Iterable[T], Iterator[T] is
        iterator: Iterator[T] => self

        to_string() -> string =>
            Ghul.Pipes.join(self)

        dispose() is
        si
    si
si
